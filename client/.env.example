# Cut the Queue Frontend Environment Configuration
# In production on Vercel, if this is not set, it safely defaults to https://cuthequeue-api.onrender.com
# In local development, if this is not set, it safely defaults to http://localhost:5001
VITE_API_URL=https://cuthequeue-api.onrender.com
